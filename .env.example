# Backend origin used during `npm run dev` and baked into the build as a fallback.
# At runtime this is overridden by public/config.json if present.
# Origin only — no trailing slash, no endpoint path.
VITE_API_BASE=http://localhost:8000
